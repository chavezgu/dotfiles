let mapleader=","

" use the listed characters to enumerate quickhint elements
set hintchars=hjklasdf

" set duckduckgo as the main search engine. 
" set defsearch=duckduckgo

" Edit the pentadactylrc 
nnoremap <Leader>ev :!gvim $HOME/.vimperatorrc<CR>

"No more reaching for the Esc key 
imap jj <Esc>

" Faster
nnoremap j 3j
nnoremap k 3k

" Play Flash
" nnoremap <Leader>f :flashplay<CR>

"Ignore google sites
" :autocmd LocationChange ^https?://(reader|mail)\.google\.com/ :normal! <C-z>

" No need for navigation bar
set gui=nonavigation
