# Random commands
alias g="git"

# Pacman
alias pacin="sudo pacman -S"
alias pacreps="sudo pacman -Ss"
alias pacupg="sudo pacman -Syu"
alias paclocs="sudo pacman -Qs"
alias pacrem="sudo pacman -Rns"

# Systemd

# Journal
alias jc="sudo journalctl"

# Global
alias global="/usr/bin/global"